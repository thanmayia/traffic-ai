def optimize_signal(vehicles):
    if vehicles > 180:
        return 90
    elif vehicles > 120:
        return 75
    else:
        return 60

data['Optimized Signal'] = data['Vehicles'].apply(optimize_signal)

print(data)